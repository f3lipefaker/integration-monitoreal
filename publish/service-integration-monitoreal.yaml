apiVersion: v1
kind: Service
metadata:
  name: service-integration-monitoreal
  namespace: viptech
spec:
  selector:
    app: integration-monitoreal
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
  type: NodePort